{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "scripts": {
    "boot": "lerna bootstrap",
    "bulmax:stories": "lerna run --scope=@bulmax/bulmax storybook --stream"
  }
}
